package dojo

class CharacterCounter {

    /**
     * Gibt eine Map mit allen genutzten Schriftzeichen im übergebenen Text als Keys zurück
     * und gibt im Value für das jeweilige Schriftzeichen an, wie oft dieses im Text vorkommt.
     * Dabei wird auch auf Leerzeichen geachtet (Angabe als "_").
     */
    fun countCharacters(text: String): Map<Char, Int> {
        return mapOf()
    }

}
